apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dice-game-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dice-game
                port:
                  number: 80
